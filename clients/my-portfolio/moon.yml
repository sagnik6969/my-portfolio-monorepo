type: "application"
language: "javascript"
platform: "node"

tasks:
  dev:
    command: "npm run dev"
    local: true

  build:
    command: "npm run build"
  
  # deploy:
  #   command: "../../scripts/deploy_fe_to_gcs.sh"
  #   deps:
  #     - 'build'
