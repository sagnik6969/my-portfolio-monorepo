type: "application"
language: "javascript"
platform: "node"

tasks:
  dev:
    command: "npm run dev"
    local: true

  build:
    command: "npm run build"
    outputs:
      - "dist/"
  
  # deploy:
  #   command: "firebase deploy --only hosting"
  #   deps:
  #     - 'build'
